# ===========================
# Performance Optimization
# ===========================
# Increase history limit (default is 2000)
set -g history-limit 10000

# Reduce escape time (fixes delay in Neovim)
set -sg escape-time 0

# Increase repeat time for resize commands
set -g repeat-time 600

# Enable focus events (needed for Neovim's autoread)
set -g focus-events on

# ===========================
# Display Settings
# ===========================
# Set 256 color support
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",*:Tc"

# Start window and pane numbering at 1 (easier to reach on keyboard)
set -g base-index 1
setw -g pane-base-index 1

# Renumber windows when one is closed
set -g renumber-windows on

# Set session to exit when last window is closed
set -g exit-empty on

# Enable mouse control
set -g mouse on

# ===========================
# Key Bindings
# ===========================
# Reload config file
bind r source-file ~/.tmux.conf \; display-message "tmux config reloaded"

# Split panes using | and -
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
unbind '"'
unbind %

# Switch panes using Alt + h/j/k/l without prefix
bind -n M-h select-pane -L
bind -n M-l select-pane -R
bind -n M-k select-pane -U
bind -n M-j select-pane -D

# Resize panes using Prefix + H/J/K/L
bind -r H resize-pane -L 5
bind -r L resize-pane -R 5
bind -r K resize-pane -U 5
bind -r J resize-pane -D 5

# ===========================
# Additional Useful Bindings
# ===========================
# Quick window switching (Alt + number)
bind -n M-1 select-window -t 1
bind -n M-2 select-window -t 2
bind -n M-3 select-window -t 3
bind -n M-4 select-window -t 4
bind -n M-5 select-window -t 5
bind -n M-6 select-window -t 6
bind -n M-7 select-window -t 7
bind -n M-8 select-window -t 8
bind -n M-9 select-window -t 9

# Copy mode vi bindings (for better Neovim consistency)
setw -g mode-keys vi
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-selection-and-cancel
bind -T copy-mode-vi C-y send -X copy-pipe-and-cancel "xclip -in -selection clipboard"

# ===========================
# Status Bar (minimal & clean)
# ===========================
set -g status-position top
# set -g status-justify left
# set -g status-style 'bg=default fg=white'
# set -g status-left ''
# set -g status-right '#[fg=white,bold] %H:%M '
# set -g window-status-current-style 'fg=cyan,bold'
